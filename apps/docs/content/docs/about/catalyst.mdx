---
title: "Project Catalyst"
description: "Proposals that we have submitted to Project Catalyst and its progress."
---
import { Status } from "@/components/ui/Status";
import { TaskList } from "@/components/ui/TaskList";
import Link from "fumadocs-core/link";
import { CatalystCard } from "@/components/ui/CatalystCard";
import { fund13, fund12, fund11, fund10 } from "@/data/catalyst";

<div>
  { fund13 && (
    <>
      <h1 className="mt-4">Fund13</h1>
      <Cards className="mb-16 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {
          fund13.map((fundItem, idx) => (
            <CatalystCard key={idx} title={fundItem.title} desc={fundItem.desc} url={fundItem.url} completed={fundItem.completed} tobecompleted={fundItem.tobecompleted} status={fundItem.status} funded={fundItem.funded} />
          ))
        }
      </Cards>
    </>
  )}

  { fund12 && (
    <>
      <h1 className="mt-4">Fund12</h1>
      <Cards className="mb-16 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {
          fund12.map((fundItem, idx) => (
            <CatalystCard key={idx} title={fundItem.title} desc={fundItem.desc} url={fundItem.url} completed={fundItem.completed} tobecompleted={fundItem.tobecompleted} status={fundItem.status} funded={fundItem.funded} />
          ))
        }
      </Cards>
    </>
  )}

  { fund11 && (
    <>
      <h1 className="mt-4">Fund11</h1>
      <Cards className="mb-16 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {
          fund11.map((fundItem, idx) => (
            <CatalystCard key={idx} title={fundItem.title} desc={fundItem.desc} url={fundItem.url} completed={fundItem.completed} tobecompleted={fundItem.tobecompleted} status={fundItem.status} funded={fundItem.funded} />
          ))
        }
      </Cards>
    </>
  )}

  { fund10 && (
    <>
      <h1 className="mt-4">Fund10</h1>
      <Cards className="mb-16 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {
          fund10.map((fundItem, idx) => (
            <CatalystCard key={idx} title={fundItem.title} desc={fundItem.desc} url={fundItem.url} completed={fundItem.completed} tobecompleted={fundItem.tobecompleted} status={fundItem.status} funded={fundItem.funded} />
          ))
        }
      </Cards>
    </>
  )}
</div>